import { View, Text } from 'react-native';

export default function DevoirScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Devoirs — Élève</Text>
    </View>
  );
}