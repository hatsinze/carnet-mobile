import { View, Text } from 'react-native';

export default function PaiementsScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Paiements — Parent</Text>
    </View>
  );
}